import { ContainsPipePipe } from './contains-pipe.pipe';

describe('ContainsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ContainsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
